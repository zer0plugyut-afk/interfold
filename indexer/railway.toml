# Railway — Dockerfile uses Node 22; Nixpacks also pinned via engines/.nvmrc/nixpacks.toml
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
